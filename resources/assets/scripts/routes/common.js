export default {
  init() {
    // document.querySelectorAll('.btn').forEach(btn => btn.append(
    //
    // ));
  },

  // JavaScript to be fired on all pages, after page specific JS is fired
  finalize() {
  },
};
